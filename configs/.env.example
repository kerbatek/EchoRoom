# Database Configuration
# Copy this file to .env and update with your PostgreSQL connection details

# Production Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=chat_app
DB_SSLMODE=disable

# Test Database (for running tests)
# Configure these for testing - tests will use these values if present
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_USER=postgres
TEST_DB_PASSWORD=your_password_here
TEST_DB_NAME=chat_app_test
TEST_DB_SSLMODE=disable

# Alternative: Test Database URL (overrides individual TEST_DB_* variables if set)
# TEST_DATABASE_URL=postgres://postgres:password@localhost:5432/chat_app_test?sslmode=disable

# For production, use sslmode=require
# DB_SSLMODE=require
# TEST_DB_SSLMODE=require